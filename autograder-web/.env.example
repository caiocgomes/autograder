# For local dev: not needed. Vite proxy forwards API calls to backend (see vite.config.ts).
# For production: set to the backend URL (e.g., https://api.myapp.com)
# VITE_API_URL=https://api.myapp.com
